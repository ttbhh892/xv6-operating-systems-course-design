并发哈希表出现键丢失的原因：

假设线程 A 和线程 B 同时向同一个哈希桶插入不同的键。两个线程可能同时读取该桶原来的链表头指针。随后，线程 A 创建新节点并将桶头修改为 A 的节点；线程 B 又将桶头修改为 B 的节点。由于线程 B 使用的仍然是最初读取的旧链表头，线程 A 插入的节点会从链表中丢失。

解决方法是为每个哈希桶设置一把互斥锁。线程修改或查找某个桶时，只获取该桶对应的锁。这样可以避免同一个桶上的数据竞争，同时允许访问不同哈希桶的线程并行执行。

